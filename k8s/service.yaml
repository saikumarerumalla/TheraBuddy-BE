apiVersion: v1
kind: Service
metadata:
  name: therabuddy-api-service
  labels:
    app: therabuddy-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: therabuddy-api
